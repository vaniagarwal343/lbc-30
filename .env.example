# LBC-30 — agent + search provider keys (reused from rb-30/.env and bm/.env)
# Claude Code (generation) + Anthropic judge cross-checks
ANTHROPIC_API_KEY=
# Codex CLI (generation) + BrowseComp grader judge
OPENAI_API_KEY=
# Search backends (MCP)
EXA_API_KEY=
VALYU_API_KEY=
GEMINI_API_URL=
GEMINI_API_TOKEN=
GEMINI_MODEL=
